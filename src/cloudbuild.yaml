steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - chorbo-webhook
  - --region=us-central1
  - --source=./src
  - --trigger-http
  - --runtime=nodejs14
